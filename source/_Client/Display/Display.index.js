module.exports = {
  type: "Type",
  extend: "Tree",
  errors: {
    display_var_not_found: "Display variable ${key} not found for ${path}",
    display_not_inherited: "${path} must inherit Display"
  }
}
