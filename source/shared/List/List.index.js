module.exports = {
  type: "Type",
  extend: "Tree",
  errors: {
    list_not_an_array: "Branch ${path} is not a List (array)",
    list_invalid_count: "List ${path} expects ${expected} values in stead of ${actual}",
  }
}
